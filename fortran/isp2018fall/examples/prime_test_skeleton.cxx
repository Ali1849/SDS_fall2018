int main() {
  // ask for how many primes
  int how_many{-1};
  cin >> how_many;
  // then cough up that many primes
  int number_of_primes_found{0};
  for ( int current_test=2; number_of_primes_found<how_many; current_test++) {
    // if current test is prime count,
    if () {....}
  }
  return 0;
}
